import React from "react";

const Navbar = () => {
  return (
    <nav className="navbar bg-primary">
      <div className="navbar-brand">REDUX</div>

      <a className="btn btn-danger">Nuevo &#43;</a>
    </nav>
  );
};

export default Navbar;
